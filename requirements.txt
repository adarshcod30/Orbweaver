# Pinned to versions verified on macOS 15 / Apple M4, Python 3.13.7.
# Nothing here needs torch; the optional GNN scorer installs it separately.
numpy==2.2.6
pandas==2.3.0
pyarrow==24.0.0
xgboost==3.2.0
scikit-learn==1.7.0
igraph==1.0.0
networkx==3.6.1
matplotlib==3.10.9
scipy==1.17.1
pydantic==2.11.3
PyYAML==6.0.3
pytest==8.4.1

# Optional: only needed for `make sage`, the alternative GNN scorer.
# torch>=2.0
# torch_geometric>=2.8
